#pragma once

// ESPHome discovers component headers independently. Keep the automation action
// definitions in programmable_load.h, after ProgrammableLoadComponent is complete,
// and make this compatibility header resolve to that canonical declaration.
#include "programmable_load.h"
